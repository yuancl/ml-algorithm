心得：
1.使用逻辑回归进行多分类使用，就会有多组h(x)值，最后对每个数据进行h(x)的比较，取最大的值作为最终的分类结果
2.重要是掌握向量化的表示和推导
3.逻辑回归不合适负责的多分类模型，当然可以使用exe2中的多项式来模拟负责模型，但是计算量很大，效率比较低，所以使用神经网络会更合适
4.神经网络做多分类模型，认清楚a，z等含义，也是有10组分类器，然后对每个数据用10组分类器进行预测，看哪个预测值最大
